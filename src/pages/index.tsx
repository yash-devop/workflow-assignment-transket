/**
 * The `/pages` directory contains only route-level page definitions.
 *
 * All feature-specific logic, components, hooks, and services
 * are sourced from the `/features` directory.
 *
 * This keeps routing concerns separate from business logic
 * and promotes a clean feature-based architecture.
 */
